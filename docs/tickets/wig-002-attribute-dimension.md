# WIG-002 — 发型客观属性表新增假发维度（留空即不可行）

**Spec**: `docs/specs/wig-assisted-hairstyle-options.md`
**Blocked by**: 无 — 可与 WIG-001 并行
**Triage**: ready-for-agent

## 交付

发型客观属性表新增假发维度，表达「该款式在假发前提下是否可行、需要哪一档工艺」。
本票**全部留空**，只落结构。

## 为什么留空也要先合

fail closed：未标注 = 不可行。所以结构就位、数据留空时，系统行为零变化、假发方案零输出。
这一票是「结构先落地、调研结论负责回填」的载体，让 WIG-005 只需改数据、不需改结构。

## 验收

- 维度的取值集合与类型有编译期约束，不是自由字符串。
- 测试锁死：维度留空时下游按「不可行」处理（数据侧的 fail closed 保证）。
- 依据强度字段就位，且**不允许**在无实拍对照时标注为「实测」。
- 全表 17 条记录结构合法。
- `npm test` 全绿，且现有推荐行为无变化。
