-- 假发可选项落成独立的推荐集合，而不是并入默认发型集合。
--
-- 为什么用新的枚举值而不是复用 hairstyle 加个标记字段：产品约束是「默认发型列表与从前
-- 逐位一致」。已有多处按 `kind = 'hairstyle'` 查询集合（预览出图的入口、编排器里的
-- 预览生成），其中还有不带排序的 findFirst —— 复用同一枚举值时，只要漏掉一处过滤，
-- 假发款就会窜进默认列表，且表现为不确定的行为。新枚举值让这件事在结构上不可能发生：
-- 那些查询自动排除假发集合，不需要改。
--
-- 纯追加，不改动任何现有行。
ALTER TYPE "RecommendationKind" ADD VALUE 'hairstyle_wig';
