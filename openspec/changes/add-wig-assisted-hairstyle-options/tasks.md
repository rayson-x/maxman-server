# 任务

**本提案是补立的**：下列已完成项在 `main` 上，未完成项才是待做的。

## 1. 领域与规则内核

- [x] 1.1 `CONTEXT.md` 定义 `Hair Volume Signal` 与 `Available Volume Premise`
- [x] 1.2 约束推导接受显式的可用发量前提；自身头发前提下行为逐位不变
- [x] 1.3 为 `rules/` 建立首次测试覆盖，钉住实测结论（尤其「仅视觉发量薄 → 无约束」）

## 2. 数据

- [x] 2.1 发型属性表新增假发可行性维度，fail closed（未标注 = 不可行）
- [x] 2.2 依据强度联合类型不含「实测」，类型层面挡住夸大
- [x] 2.3 按工艺调研回填 17 款标注（3 款不可行，7 款需前额工艺档位）
- [x] 2.4 需要烫卷的款式带材质限制说明

## 3. 假发方案推导（唯一新增接缝）

- [x] 3.1 纯函数：排除集 + 模型通道排序 → 假发方案
- [x] 3.2 三条开放条件；任一不成立即不开放，并给出可排查的原因
- [x] 3.3 形态匹配取「被什么挡住」与属性表标注更严的一方
- [x] 3.4 fail closed 并产出标注缺口
- [x] 3.5 规范名优先于别名解析，避免别名碰撞把两个款式塌成一条

## 4. 可行集与接线

- [x] 4.1 排除项带出款式名与两项可行性属性
- [x] 4.2 在发型推荐这一步接线，不新增模型调用
- [x] 4.3 达成路径随方案落地写进变更清单

## 5. 可选中

- [x] 5.1 `RecommendationKind` 新增 `hairstyle_wig`（纯追加迁移）
- [x] 5.2 选定行为与普通发型一致（写已选发型、校验风格方向、失效穿搭）
- [x] 5.3 选定接口同时接受默认发型与假发款
- [x] 5.4 方案落地接受该集合类型
- [x] 5.5 风格方向变更使假发集合失效
- [x] 5.6 幂等：跟随者等创建者写完；回收重跑先清旧候选；事务失败标 failed 而非留在 preparing

## 6. 未完成

- [x] 6.1 预览出图按范围取集合：假发款**只在用户点开入口后**才出图（出图付费，
      而假发是用户可能不选的路径）。不带范围时行为与今天逐位一致，且照走既有容量限流。
      无法识别的范围返回 400，不静默当成默认 —— 否则客户端会以为请求了假发那批，
      实际在为默认列表再花一次钱。
      **验证受阻**：配套的路由测试写好了但跑不了 —— 目录快照数据有 13 个源而代码源注册表
      只认 12 个（`Snapshot source missing: fitRulesBodyOutfit`），任何加载快照的测试都起不来。
      这是另一处在飞的改动，不属于本能力；那边补完后需要补跑这批路由测试。
- [x] 6.2 客户端入口与呈现（默认列表不变；入口纯文字；点开才为这批出图；选中走同一调用）
- [x] 6.3 「显式确认」旁路**已移除**而非实现：它按构造不可能被触发（服务的是没自报困扰、
      却愿意用假发的用户，而这类用户看不到入口）。加字段+端点只会造出无调用方的表面。
      要覆盖这批人需要一个新的表达入口，属另一个产品决定。
- [x] 6.4 假发任务落阶段 1（非阶段 0）：网购成品仍需修剪才贴合，从下单到成型要几天。
      落地规则本来就是 stage1，已补测试钉住，避免有人凭直觉挪到阶段 0。
- [ ] 6.5 那 7 款「整顶 + 前额工艺」的实拍补强 —— 当前依据最薄弱处
- [ ] 6.6 目录补齐「高发量需求」标注：现状 low 6 / medium 21，无一为 high，导致自报困扰
      推出的「排除 high 档」什么也排不掉 —— 自报这条路解锁不了任何款式，且「只补量感」
      档位与其材质注意事项都不可达。属数据缺口。
- [ ] 6.7 校准渲染表与运行时目录命名对齐（当前零命中），假发款才能有预览图与阶段目标图。
- [ ] 6.8 exploration（模型独有候选）绕过发量可行性过滤，属双源推荐那一侧的既有缺口。
      本能力已通过「默认列表已提供的款式不进假发入口」避免与它矛盾，但那个洞本身仍在。
